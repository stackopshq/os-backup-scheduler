# Pin exact versions for reproducible builds.
# Update periodically and test before releasing a new image tag.
#
# stackops-cloud is the shared provider library (OpenStack transport,
# inventory and the snapshot -> temp volume -> backup sequence). Public
# repository, pinned to a commit until it is published to a package index.
stackops-cloud @ git+https://git.stackops.ch/stackops/stackops-cloud.git@6bd4649
zabbix-utils~=2.0
